# Secretos y configuracion local
.env
.env.*
!.env.example
!.env.*.example
*.key
*.p12
*.pfx
*.pem

# Datos locales, embeddings y archivos de ejecucion
*.db
*.sqlite
*.sqlite3
*.log
/data/

# Configuracion personal del editor/SO
.DS_Store
Thumbs.db
.idea/
.vscode/

# El documento fuente se mantiene local; solo se versionan los entregables.
doc/*
!doc/operacion/
doc/operacion/*
!doc/operacion/catalogo_perfiles_ejecutivos.pdf
!doc/rag/
doc/rag/*
!doc/rag/*.pdf
!doc/rag/manifest.json

# make check / make test report
.make-report.tsv

# Reportes de una corrida del harness de evaluacion (dashboard HTML, JSON, scorecard).
# Cada corrida produce artefactos nuevos que dependen del estado del backend y de las
# respuestas del modelo; no se versionan.
backend/evals/reports/
backend/evals/scorecard.md
